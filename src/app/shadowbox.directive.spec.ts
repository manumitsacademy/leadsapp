import { ShadowboxDirective } from './shadowbox.directive';

describe('ShadowboxDirective', () => {
  it('should create an instance', () => {
    const directive = new ShadowboxDirective();
    expect(directive).toBeTruthy();
  });
});
