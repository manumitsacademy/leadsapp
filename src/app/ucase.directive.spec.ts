import { UcaseDirective } from './ucase.directive';

describe('UcaseDirective', () => {
  it('should create an instance', () => {
    const directive = new UcaseDirective();
    expect(directive).toBeTruthy();
  });
});
